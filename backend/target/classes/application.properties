spring.application.name=backend

# PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/kasicircle
spring.datasource.username=thando_mthethwa
spring.datasource.password=KasiConnect123

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# PostgreSQL Dialect
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Server
server.port=8081

# Logging
logging.level.org.springframework.security=DEBUG

# JWT Configuration
jwt.secret=MySuperSecretKeyForKasiCircleApplication2026ThatIsAtLeast32CharactersLong
jwt.expiration=86400000

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.schema=DEBUG
logging.level.org.hibernate.tool.schema=DEBUG